# 2026-08-17 — L7 WAY / AVLI Cloud

Open work (Founder): **integrate L7 WAY with avli_cloud**. Required, not optional.

## Architecture (do not reopen)

ADR 0002 / Law I stays closed:

- L7 is the public control plane: `serve.js` on `127.0.0.1:18793` (OpenClaw keeps `:18789`).
- AVLI is private workers and experience: `~/avli_cloud`.
- `~/.l7` is operator runtime.
- Front-facing copy is AVLI Cloud Campaign / Team / Organization. No esoteric language on Studio/offers.

## Already proven (Founder loop)

- AVLI echo worker (`packages/avli-worker-sdk/examples/echo_worker.py`) on loopback `:18792` through `lib/avli-worker-client.js` (`text.echo`).
- Hostinger n8n via SSH reverse tunnel + docker-bridge (`172.18.0.1:18793`) with `L7_API_TOKEN` + `L7_API_TENANT_ID=tenant:service`.
- `./start.sh` is the only Founder Loop boot. It does **not** call `~/avli_cloud/start.sh` (that file is the Hostinger docker advisor stack).
- Smoke 6/6 when Gateway is live.

## Remaining integration

- Worker SDK through Gateway jobs beyond echo (slice 3: Mac Ollama `text.generate`; image/video stay local adapters).
- Control-plane compose stays memory-fit / parked — do not deploy the 40-service compose or `compose.control-plane.yml` on the 8 GB VPS.
- Landing and Studio are the same product as avli-landing offers (Campaign $42 / Team $420 / Organization custom).
- Secrets/tokens live in vault + `~/avli_cloud/deploy/secrets/` (gateway token, echo worker token). Never commit them.
- Slice 4+ still open: secret rotation, qdrant-or-reuse, registry/quotas/OTEL, NCLS restore, assistant-ui, remaining modalities.

## Shipped this session (2026-08-17)

- Slice 3: Mac Ollama worker `text.generate` through Gateway `/v1/jobs` (`ollama_worker.py` on `:18798`, models already present: `gemma4:26b`). Image/video stay local mock/media adapters.
- Docs: L7 README + implementation plan Phase notes; avli `CURRENT_ARCHITECTURE.md` one-slice pointer. Landing/Studio Campaign $42 / Team $420 / Organization aligned.
- Live Gateway recovered on `:18793`. Smoke 6/6. OpenClaw `:18789` not stolen. 40-service compose not deployed.

## Task 1 — avli-landing public brand (repo, not live)

Repo `avli_cloud/services/avli-landing` now matches the creation brand:

- Header: Plans, Workspace, Trust, Mail. Header CTA is **Sign in** (Authentik), never Start in Studio.
- Open Studio is `#share` (workspace), not `auth.avli.cloud`.
- Team CTA: **Talk to us** (mailto). Campaign $42 / Team $420 / Organization custom.
- `/constellation` and `/signal` redirect to `/` with noindex.
- Body tokens: void `#0C0A08` / gold `#C4A35A` / parchment `#E4D6B8`. Geist/Manrope/indigo are off the live face.
- Tests: `npm test` in `services/avli-landing` — 3/3 pass.

**avli.cloud was not redeployed.** Live container is still `avli-landing:2026-08-01` (2 weeks). Local Docker is missing; a Next build on the VPS with ~2.3 GiB available and no swap is an OOM risk to other stacks. `compose.yml` is tagged `avli-landing:2026-08-17`. Isolated deploy still needed: rsync to `/opt/avli/landing`, then `docker compose build && docker compose up -d` from that compose file only.

Do not steal OpenClaw `:18789`. Do not touch the Orisha LTX video tree.
